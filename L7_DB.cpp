//Declare 3 variables for name, age, gender
//cout object to display the message
//cin object to get the input
//write 1st if condition to check the age
    // age < 18 -> No
    // age > 60 -> No
    // 18 < age <= 60 -> yes
    //write 2nd if condition to check the gender
        // gender == Male -> G/P/B
        // gender == female -> A/S/P